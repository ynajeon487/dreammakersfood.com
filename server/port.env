# Cấu hình server
PORT=4000

# Cấu hình database (PostgreSQL)
DATABASE_URL=postgresql://user:pass@localhost:5432/dbname

# JWT secret (nên thay bằng một chuỗi dài, ngẫu nhiên, khó đoán)
JWT_SECRET=thay_bang_mot_chuoi_dai_va_bao_matru

# Các biến khác tuỳ dự án